<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}!{{else}}Eaaaat Sommeething!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE or feed to dog!</button>
</li>
